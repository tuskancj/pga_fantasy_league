cells:
  - kind: 2
    languageId: sql
    value: "select distinct course.course_name\r

      from round\r

      inner join event on round.id_event = event.id_event\r

      inner join course on round.id_course = course.id_course\r

      where event.event_name like '%Waste%'\r

      limit 50;"
    metadata: {}
  - kind: 2
    languageId: sql
    value: "select distinct event.calendar_year\r

      from round\r

      inner join course on round.id_course = course.id_course\r

      inner join event on round.id_event = event.id_event\r

      where course.course_name = 'TPC Scottsdale'"
    metadata: {}
  - kind: 2
    languageId: sql
    value: "select DISTINCT event.event_name\r

      from round\r

      inner join event on round.id_event = event.id_event\r

      inner join course on round.id_course = course.id_course\r

      where course.course_name in ('Torrey Pines (South)', 'Torrey Pines Golf
      Course (South Course)')\r

      limit 10;"
    metadata: {}
  - kind: 2
    languageId: sql
    value: select * from event where event_name like '%WM%'
    metadata: {}
metadata:
  conn:
    id: YpYSJVulh5NJZczT6RjoJ
    name: db-pga-fantasy
  database: postgres
  schema: public
